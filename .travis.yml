language: objective-c
osx_image: xcode9.1
install:
- bundle install --path vendor/bundle
script:
- echo "Hello World"
- set -o pipefail && xcodebuild -project SnapshotTest.xcodeproj  -scheme "SnapshotTest-iOS" -configuration "Debug" -sdk "iphonesimulator" -destination "name=iPhone 7" clean test | xcpretty
